<%= form_for(@place_service) do |f| %>
  <% if @place_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@place_service.errors.count, "error") %> prohibited this place_service from being saved:</h2>

      <ul>
      <% @place_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dry_cleaning %><br>
    <%= f.check_box :dry_cleaning %>
  </div>
  <div class="field">
    <%= f.label :public_transport %><br>
    <%= f.text_field :public_transport %>
  </div>
  <div class="field">
    <%= f.label :other %><br>
    <%= f.text_field :other %>
  </div>
  <div class="field">
    <%= f.label :place_id %><br>
    <%= f.text_field :place_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
